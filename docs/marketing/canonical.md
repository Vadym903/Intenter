Intenter is a local, deterministic permission layer for AI coding agents that approves what a command actually does, not the string it was typed as.

<!--
Line 1 of this file is the canonical one-sentence definition of Intenter.
It must appear verbatim in: README.md (opening), the GitHub repository
description, llms.txt (blockquote), assets/social/preview.svg and
docs/marketing/pitch.md. `scripts/check-readme.sh` enforces the README, llms.txt,
pitch and preview copies; docs/marketing/repo-settings.md carries it into the
repository description.

Change it here first, then everywhere else, in the same commit.
Allowed variants: none.
-->
