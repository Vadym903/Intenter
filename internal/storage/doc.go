// Package storage owns the SQLite database: connection setup, forward-only
// migrations and the repositories the daemon writes through
// (PROTOTYPE_SPEC.md §23, contracts/storage-schema.md).
package storage
