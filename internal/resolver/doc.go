// Package resolver turns parsed commands into resolved actions: command
// recognizers, script/wrapper resolution, workspace context and fingerprints
// (PROTOTYPE_SPEC.md §15, §16.2).
package resolver
