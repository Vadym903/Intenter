<!-- Explain why this change is needed. The what is in the diff. -->

## What this changes

## Why

<!-- Link the issue or discussion if there is one: Fixes #123 -->

## Checklist

- [ ] Tests cover the behavior change, in the style of the tests next to the code.
- [ ] `make test` and `make lint` pass locally (CI runs them on all three platforms).
- [ ] `make docs` was run and `docs/cli/` committed, if a command, flag or help string changed.
- [ ] `make docs-check` passes (links, markdown style, badges, README rules).
- [ ] A `CHANGELOG.md` entry exists under `## [Unreleased]`.
- [ ] Specification changes, if any, are recorded in `specs/001-agentguard-prototype/PROTOTYPE_SPEC.md` (Appendix C for contradictions found while implementing).
- [ ] I agree to the [contribution terms](../CONTRIBUTING.md#contribution-terms) and every commit is signed off (`git commit -s`).
